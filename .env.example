# ============================================================================
# COOITZA — Portal de Promotores
# Copiá este archivo a .env.local y completá los valores marcados con TODO.
# ============================================================================

# --- Supabase COOITZA (proyecto propio: auth, profiles, puntos, premios) -----
# URL y anon key son PÚBLICOS (se exponen al browser; RLS protege los datos).
NEXT_PUBLIC_SUPABASE_URL="https://bjkosfyuzcixdwzjswak.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImJqa29zZnl1emNpeGR3empzd2FrIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODEwNTUwMzUsImV4cCI6MjA5NjYzMTAzNX0.aCSa-omD38iHvTVpl5vodPAaXfjDQkVO7gCl7Opi9kg"

# SECRETO — SOLO en el servidor. Nunca con prefijo NEXT_PUBLIC_, nunca en el repo.
# Pegalo en Vercel → Project Settings → Environment Variables (Production + Preview).
SUPABASE_SERVICE_ROLE_KEY="TODO: pegar el service_role de Cooitza solo en Vercel"

# --- Supabase VXM (BD compartida: catálogo, leads CRM, pipeline, rendimiento) -
# El catálogo se lee con el anon de VXM (read-only, filtrado por is_public_cooitza).
NEXT_PUBLIC_VXM_SUPABASE_URL="https://fuyqhcswcluvozkfluwg.supabase.co"
NEXT_PUBLIC_VXM_SUPABASE_ANON_KEY="TODO: anon key de VXM (público, read-only)"

# SECRETO — para ESCRIBIR leads en crm_leads de VXM desde Server Actions.
# Solo en Vercel server env. Se usa en Fase 3 (leads). Por ahora opcional.
VXM_SUPABASE_SERVICE_ROLE_KEY="TODO: service_role de VXM, solo en Vercel (Fase 3)"

# --- Config pública --------------------------------------------------------
NEXT_PUBLIC_SITE_URL="https://newcooitza.viajexmundo.com"
NEXT_PUBLIC_AGENCY_NAME="Viajexmundo"
NEXT_PUBLIC_WHATSAPP_NUMBER="50230149000"

# --- Google OAuth (OPCIONAL, degradable) -----------------------------------
# Poné "true" SOLO cuando hayas habilitado el provider Google en Supabase Auth.
# Si no está en "true", el login muestra solo correo/contraseña (sin botón Google).
NEXT_PUBLIC_GOOGLE_AUTH_ENABLED="false"

# --- Cron (sync de pipeline) -----------------------------------------------
# SECRETO. Vercel lo envía como Authorization: Bearer en cada corrida del cron.
# Generá uno con: openssl rand -hex 32 . Solo en Vercel. En prod es obligatorio.
CRON_SECRET="TODO: generar y pegar solo en Vercel"

# --- Resend (email, OPCIONAL y degradable) ---------------------------------
# Si faltan, el portal NO envía emails y nada falla. Conectalos cuando quieras.
RESEND_API_KEY="TODO: re_... solo en Vercel (opcional)"
RESEND_FROM="Viajexmundo <portal@viajexmundo.com>"
